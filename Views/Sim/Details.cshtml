@model Sim
@{
    ViewBag.Title = "Sim Details";
}

<h1>SIM Details</h1>
<div class="row">
    @*<div class="col-sm-4">
        <img src="~/images/@ViewBag.ImageFilename" 
             alt="@Model.Name Image" 
             class="img-responsive center-block"/>
    </div>*@

    <div class="col-sm-8">
        <h2>@Model.Name</h2>
        <table class="table table-bordered table-striped">
            <tr>
                <td>Abstract of SIM:</td>
                <td>@Model.Abstract</td>
            </tr>
            <tr>
                <td>Information about the SIM:</td>
                <td>@Html.Raw(Model.Info)</td>
            </tr>
        </table>
        
    </div>
</div>