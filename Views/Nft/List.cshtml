@*Cepada Morgan List View*@
@model NftListViewModel
@{
    ViewBag.Title = "Nft List";
}

<h1>Cepada's NFT List</h1>
<div class="row">
    <div class="col-sm-2">
            <a asp-controller="Nft" 
                asp-action="List" 
                asp-route-id="All" 
                class="list-group-item @Model.CheckActiveCategory("All")">
                All
            </a>

            @foreach (Category c in Model.Categories)
            {

                    <a asp-controller="Nft" 
                     asp-action="List" 
                     asp-route-id="@c.Name" 
                     class="list-group-item @Model.CheckActiveCategory(c.Name)">
                        @c.Name
                    </a>
            }
    </div>

    <div class="col-sm-10">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Price</th>
                    <th>Details</th>
                    
            </thead>
            <tbody>
                @foreach (var nft in Model.Nfts)
                {
                    <tr>
                        <td>@nft.Name</td>
                        <td>@nft.Price.ToString("C")</td>
                        <td>
                            <a asp-controller="Nft" 
                               asp-action="Details"
                               asp-route-id="@nft.NftID"
                               asp-route-slug="@nft.Slug">View Details</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>